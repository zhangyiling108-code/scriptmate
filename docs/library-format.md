# Library Metadata Format

CSV header:

```csv
path,title,description,tags,category
```

- `path` is relative to the library root.
- `tags` uses `|` separators.
- `category` helps fallback grouping and search ranking.
